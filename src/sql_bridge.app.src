%% vi: ts=4 sw=4 et ft=erlang
{application, sql_bridge,
    [
        {description, "A convenience layer for SQL databases"},
        {vsn, "0.1.0"},
        {registered, []},
        {applications, [
                kernel,
                stdlib
            ]},
        {env, []},
        {mod, {sql_bridge_app, []}}
    ]
}.
